<Window x:Class="Lock.Views.InstalledAppPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource AppWindow}"
        Title="从已安装的程序选择"
        Width="800" Height="560"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource InfoBox}" Margin="0,0,0,12">
            <TextBlock Style="{StaticResource Muted}" Foreground="{StaticResource Text}"
                       Text="来源：控制面板“程序和功能”+ 开始菜单。控制面板不记录主程序路径，这里是推断结果，请核对“主程序”一列；灰色的是没找到主程序的，无法添加。"/>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" MaxWidth="360" HorizontalAlignment="Left" Width="360">
                <TextBox x:Name="SearchBox" Padding="30,6,9,6" TextChanged="SearchBox_TextChanged"/>
                <Path Data="M9,3 a6,6 0 1 0 0,12 a6,6 0 1 0 0,-12 M13.5,13.5 L18,18" Stroke="{StaticResource TextMuted}" StrokeThickness="1.6"
                      Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0" IsHitTestVisible="False"/>
                <TextBlock Text="搜索程序名、发布者或路径" Foreground="{StaticResource TextDisabled}" Margin="32,0,0,0"
                           VerticalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            <TextBlock x:Name="CountText" Grid.Column="1" VerticalAlignment="Center" Style="{StaticResource Muted}"/>
        </Grid>

        <ListView x:Name="AppList" Grid.Row="2" SelectionMode="Extended" MouseDoubleClick="AppList_MouseDoubleClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource {x:Type ListViewItem}}">
                    <Setter Property="IsEnabled" Value="{Binding HasExe}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="程序" Width="240">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding Icon}" Width="20" Height="20" Margin="4,0,10,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Publisher}" Foreground="{StaticResource TextMuted}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="主程序" Width="320">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ExePath}" Foreground="{StaticResource TextMuted}" TextTrimming="CharacterEllipsis" ToolTip="{Binding ExePath}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="来源" Width="170">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Source}" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="取消" MinWidth="80" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="添加所选" Style="{StaticResource PrimaryButton}" MinWidth="100" IsDefault="True" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
